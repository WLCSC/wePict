<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Wepict" %></title>
    <%= csrf_meta_tags %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="//cdnjs.cloudflare.com/ajax/libs/html5shiv/3.6.1/html5shiv.js" type="text/javascript"></script>
    <![endif]-->

    <%= stylesheet_link_tag "application", :media => "all" %>

    <%= javascript_include_tag "application" %>
  </head>
	<body style="background-image: url('<%= image_path 'dark_wall.png' %>');">

    <div class="navbar navbar-fixed-top navbar-inverse">
      <div class="navbar-inner">
				<div class="container">
					<%= link_to root_path, :class => 'brand' do %>
						<span class="bb-red">W</span>
						<span class="bb-orange">e</span>
						<span class="bb-yellow">P</span>
						<span class="bb-green">i</span>
						<span class="bb-blue">c</span>
						<span class="bb-violet">t</span>
					<% end %>
        </div>
      </div>
    </div>

    <div class="container">
        <div class="row">
					<div class="span10 offset1">
						<div class="well main">
							<div class="btn-toolbar">
								<% if current_user %>
									<div class="btn-group">
										<%= link_to "Log Out " + current_user.username, sign_out_path, :class => 'btn btn-warning' %>
										<%= link_to "Home", root_path, :class => 'btn btn-primary' %>
										<%= link_to "All Rooms", rooms_path, :class => 'btn btn-success' %>
									</div>
									<% if current_user.admin? %>
										<div class="btn-group">
											<%= link_to "Users", users_path, :class => 'btn' %>
											<%= link_to "Groups", groups_path, :class => 'btn' %>
										</div>
									<% end %>
								<% end %>
							</div>
							<div id="flashbox"><%= bootstrap_flash %></div>
							<%= yield %>
					</div>
          </div>
        </div><!--/row-->

      <footer class="pull-right">
			<p>Created by Brad Thompson, WLCSC Technology Department<br/>Inspired by Leo Leffert</p>
      </footer>

    </div> <!-- /container -->

    <!-- Javascripts
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->

  </body>
</html>
