<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="utf-8">
		<meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
		<meta name="viewport" content="width=device-width, initial-scale=1.0">
		<title><%= content_for?(:title) ? yield(:title) : "Wepict" %></title>
		<%= csrf_meta_tags %>

		<!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
		<!--[if lt IE 9]>
		<script src="//cdnjs.cloudflare.com/ajax/libs/html5shiv/3.6.1/html5shiv.js" type="text/javascript"></script>
		<![endif]-->

		<%= stylesheet_link_tag "application", :media => "all" %>
		<%= javascript_include_tag "application" %>
	</head>
	<body style="background-image: url('<%= image_path 'dark_wall.png' %>'); padding-top: 0px;">

		<div class="container brand main">
			<span class="bb-red">W</span>
			<span class="bb-orange">e</span>
			<span class="bb-yellow">P</span>
			<span class="bb-green">i</span>
			<span class="bb-blue">c</span>
			<span class="bb-violet">t</span>
		</div>

		<div class="container">
			<div class="row">
				<div class="span10 offset1">
					<div class="well main">
						<div id="flashbox"><%= bootstrap_flash %></div>
						<%= form_tag prompt_room_path(@room), :remote => true, :style => 'margin-bottom: 4px;' do %>
							<span style="font-weight: bold;">Set Prompt</span>
							<%= text_field_tag :content %>
							<%= submit_tag "Post Prompt", :class => 'btn btn-primary' %>
						<% end %>

						<%= render :partial => 'buttonbar' %>
						
					</div>
				</div>
			</div><!--/row-->
		</div> <!-- /container -->

		<!-- Javascripts
		================================================== -->
		<!-- Placed at the end of the document so the pages load faster -->
		<script type="text/javascript">
			$(document).ready(function(){
					setTimeout(function(){
						$('#flashbox  .alert').hide('fast', function(){ $target.remove(); });
						}, 5000);
					})
</script>

			</body>
		</html>
